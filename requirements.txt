Flask==3.0.3
gunicorn==22.0.0
requests==2.32.3
line-bot-sdk==3.11.0
google-generativeai==0.8.3

# --- Supabase ---
# IMPORTANT: pin ONLY this package. Do NOT add separate lines for
# gotrue / postgrest / httpx / realtime / supabase-auth below.
# supabase-py declares its own compatible versions of those internally,
# and manually pinning them is the #1 cause of:
#   TypeError: Client.__init__() got an unexpected keyword argument 'proxy'
# If you had any of those packages pinned in your old requirements.txt,
# delete those lines.
supabase==2.31.0
